// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://resistlist.us',
  base: '/resistlist', // Not needed if deploying to username.github.io
  // other config options...
});